#808080 - A medium gray color
#AAAAAA - A light gray color
#666666 - A dark gray color
#CCCCCC - A pale gray color
#333333 - A charcoal gray color
#444444 - A dark slate gray color
#999999 - A medium dark gray color
#F7F7F7 - A very light gray color
#E5E5E5 - A light gray color with a slight blue undertone
#D3D3D3 - A medium light gray color
import { useEffect } from "react";
import { useRouter } from "expo-router";
import { supabase } from "./lib/supabase-client";
import { StyleSheet, Text, TouchableOpacity, View } from "react-native";
import Colors from "../utils/Colors";
import { Image } from "react-native";

export default function HomePage() {
  const router = useRouter();
  useEffect(() => {
    handleGetStarted();
  }, []);
  const handleGetStarted = async () => {
    supabase.auth.onAuthStateChange((_event, session) => {
      if (session) {
        router.replace("/(tabs)/home/");
      } else {
        console.log("no user");
      }
    });
  };

  return (
    <View style={{ display: "flex", alignItems: "center" }}>
      <Image source={require("../assets/cert.png")} style={styles.welcomeImg} />
      <View style={styles.bgBottom}>
        <Text style={styles.heading}>Budget Planning App!</Text>
        <Text style={styles.paragraph}>
          Stay on track Event by Event: Your Budget Tracker App
        </Text>
        <View style={styles.ButtonContainer}>
          <TouchableOpacity onPress={() => router.replace("/(auth)/login")}>
            <Text style={styles.ButtonText}> Get Started</Text>
          </TouchableOpacity>
        </View>
      </View>
    </View>
  );
}
const styles = StyleSheet.create({
  ButtonText: {
    padding: 5,
    fontSize: 18,
    color: "white",
    fontFamily: "poppins-bold",
    alignSelf: "center",
  },
  ButtonContainer: {
    backgroundColor: Colors.BLACK,

    borderRadius: 99,
    paddingVertical: 8,
    paddingHorizontal: 25,
    margin: 8,
    alignSelf: "center",
    elevation: 10,
  },

  heading: {
    fontSize: 25,
    fontFamily: "poppins-bold",
    marginBottom: 10,
    textAlign: "center",
    color: Colors.WHITE,
  },
  paragraph: {
    fontSize: 15,
    fontFamily: "poppins",
    marginBottom: 40,
    textAlign: "center",
    color: "white",
  },
  getstarted: {
    backgroundColor: "#4caf50",
    color: "#ffffff",
    padding: 10,
    paddingHorizontal: 20,
    fontSize: 18,
  },
  welcomeImg: {
    marginTop: 90,
    width: 200,
    height: 200,
    borderColor: "black",
    backgroundSize: "cover",
    backgroundPosition: "center",
  },
  bgBottom: {
    backgroundColor: Colors.BLACK,
    width: "95%",
    height: 280,
    marginTop: 90,
    padding: 25,
    borderRadius: 25,
    marginBottom: 10,
    elevation: 1,
  },
});

//
 Alert.alert("Are you sure", "Do you really want to delete?", [
      {
        text: "Cancel",
        style: "cancel",
      },
      {
        text: "Yes",
        style: "destructive",